// Ambient Roblox datatype globals (UDim2/Color3/Enum/…) for editor/typecheck.
// The loom plugin installs them at runtime; this only pulls in their types. vide
// uses no JSX, so unlike the react example this needs no intrinsic-element types.
import "@loom-dev/preview/globals";
